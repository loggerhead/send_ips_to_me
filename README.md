#Install
Change `HOST` in `send_ips_to_me.py` to you server host.

```shell
sudo ./install.sh
```

#Usage
##Client
Restart your client or `nohup send_ips_to_me.py &`

##Server
```shell
./listenServer.py
```
